"use client"; import { Globe, Sparkles } from "lucide-react";
import { useState } from "react"; import type { ProductFormState } from "./sellerWorkspaceUtils";
import { importProductDraftRequest } from "./workspaceApi"; interface ProductAiAutocompleteProps { onApply: (nextState: ProductFormState) => void;
} export function ProductAiAutocomplete({ onApply,
}: ProductAiAutocompleteProps) { const [sourceUrl, setSourceUrl] = useState(""); const [feedbackMessage, setFeedbackMessage] = useState<string | null>(null); const [isAutocompleting, setIsAutocompleting] = useState(false); async function handleAutocomplete() { setFeedbackMessage(null); try { const normalizedUrl = sourceUrl.trim(); if (!normalizedUrl) { throw new Error("Paste a product page URL first."); } new URL(normalizedUrl); setIsAutocompleting(true); const importDraft = await importProductDraftRequest([normalizedUrl]); onApply({ brand: importDraft.draft.brand, category: importDraft.draft.category, comparisonSnippetMarkdown: importDraft.draft.comparisonSnippetMarkdown, detailsMarkdown: importDraft.draft.detailsMarkdown, idleMediaUrl: importDraft.draft.idleMediaUrl ?? "", name: importDraft.draft.name, sourceUrls: importDraft.draft.sourceUrls, }); setFeedbackMessage( importDraft.warnings.length > 0 ? importDraft.warnings.join(" ") : "AI import completed. Review the generated fields before saving.", ); } catch (error) { setFeedbackMessage( error instanceof Error ? error.message : "Could not autocomplete the product form.", ); } finally { setIsAutocompleting(false); } } return ( <section className="rounded-[1.5rem] border border-border/80 bg-primary/[0.04] p-5 shadow-[0_20px_55px_-35px_rgba(37,99,235,0.55)]"> <div className="flex flex-col gap-4"> <div className="flex items-start gap-3"> <div className="rounded-2xl bg-primary/12 p-3 text-primary"> <Sparkles className="h-5 w-5" /> </div> <div> <h2 className="font-display ui-text-large font-semibold tracking-tight"> Autocomplete with AI </h2> <p className="mt-1 ui-text-small leading-6 text-muted-foreground"> Paste the product page URL from the brand or seller site and use the live backend import flow to prefill the form. </p> </div> </div> <div className="flex flex-col gap-3 sm:flex-row"> <div className="relative flex-1"> <Globe className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /> <input type="url" value={sourceUrl} onChange={(event) => setSourceUrl(event.target.value)} placeholder="https://brand.com/products/device-name" className="w-full rounded-xl border border-input bg-background px-11 py-3 ui-text-small focus:outline-none focus:ring-2 focus:ring-primary/50" /> </div> <button type="button" onClick={() => void handleAutocomplete()} disabled={isAutocompleting} className="inline-flex min-w-40 items-center justify-center rounded-xl bg-primary px-5 py-3 ui-text-small font-semibold text-primary-foreground transition-opacity hover:opacity-90 disabled:opacity-50" > {isAutocompleting ? "Autocompleting..." : "Autocomplete"} </button> </div> <p className="ui-text-small leading-5 text-muted-foreground"> This uses the real product-import endpoint and keeps the generated result editable before you save it. </p> {feedbackMessage ? ( <div className="rounded-xl border border-border bg-background/75 px-4 py-3 ui-text-small text-muted-foreground"> {feedbackMessage} </div> ) : null} </div> </section> );
}
